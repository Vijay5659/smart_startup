import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from 'react-hot-toast'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Smart Startup Platform - AI-Guided Entrepreneurship',
  description: 'Empower aspiring entrepreneurs with AI-guided business opportunities, funding connections, and community-driven startup solutions.',
  keywords: 'startup, entrepreneurship, business, AI, community, rural, funding, microfinance',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster position="top-right" />
      </body>
    </html>
  )
} 